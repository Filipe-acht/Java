package oo.heranca;

public enum Direcao {
	/*
	 * ENUM(enumeracao) -> serve para quando temos uma quantidade definidade de possibilidades(tambem define um tipo):
		** meses do ano;
		** dias da semana.
	*/
	NORTE, LESTE, SUL, OESTE;
}
